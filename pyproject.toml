[project]
name = "staqs-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "atmoz",
]

[tool.uv.sources]
atmoz = { git = "https://github.com/moroots/atmoz.git" }
